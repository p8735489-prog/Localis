package com.localaisearch.ui.components

/** User-selectable reasoning switch. Coding behavior is inferred from the user's request. */
enum class ReasoningMode(val id: String) {
    OFF("off"),
    THINKING("thinking")
}
